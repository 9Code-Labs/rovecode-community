# Contributing

Discuss substantial changes in an issue first. Fork the repository, branch from `main`, keep changes focused, and add tests. Run `bun install --frozen-lockfile`, `bun run typecheck`, `bun test`, and `bun run build:cli`. Never commit credentials or recordings. By contributing you agree that your contribution is licensed under AGPL-3.0-only. Public APIs require compatibility notes and a changelog entry.
